<section class="section-shell"><div class="page-width"><h1 class="section-title">Addresses</h1>{% paginate customer.addresses by 8 %}{% for address in customer.addresses %}<div class="value-card">{{ address | format_address }}{% if address == customer.default_address %}<p>Default</p>{% endif %}</div>{% endfor %}{% render 'pagination', paginate: paginate %}{% endpaginate %}</div></section>
{% schema %}{ "name": "Main addresses", "settings": [] }{% endschema %}
